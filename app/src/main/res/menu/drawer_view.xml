<?xml version="1.0" encoding="utf-8"?>

<!--KOLOR IKONEK #474747-->
<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <group android:checkableBehavior="single"
            android:id="@+id/user_not_logged">

        <item
            android:id="@+id/home_logout"
            android:icon="@drawable/ic_home"
            android:title="Strona główna" />
        <item
            android:id="@+id/login"
            android:icon="@drawable/ic_person"
            android:title="Zaloguj się" />

    </group>

    <group android:checkableBehavior="single"
        android:id="@+id/user_logged">

        <group android:id="@+id/group_home">
            <item
                android:id="@+id/home_login"
                android:icon="@drawable/ic_home"
                android:title="Strona główna" />
            <item
                android:id="@+id/add_offer"
                android:icon="@drawable/ic_add_box"
                android:title="Dodaj ofertę" />
        </group>

        <group android:id="@+id/group_offers">
            <item
                android:id="@+id/offerts"
                android:icon="@drawable/ic_business_center"
                android:title="Moje oferty" />
            <item
                android:id="@+id/transactions"
                android:icon="@drawable/ic_group"
                android:title="Propozycje transakcji" />
            <item
                android:id="@+id/transaction_history"
                android:icon="@drawable/ic_history"
                android:title="Historia transakcji" />
        </group>

        <group android:id="@+id/group_account">
            <item
                android:id="@+id/account_settings"
                android:icon="@drawable/ic_settings"
                android:title="Ustawienia konta" />
            <item
                android:id="@+id/change_password"
                android:icon="@drawable/ic_security_black_24dp"
                android:title="Zmień hasło" />
            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_exit_to_app"
                android:title="Wyloguj się" />
        </group>

    </group>

</menu>